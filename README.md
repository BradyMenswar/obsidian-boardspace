# Boardspace

Boardspace is an Obsidian community plugin scaffold that uses React for a custom note view.

## Development validation

Run `npm test`, `npm run lint`, and `npm run build`. Before release, complete the [schema-v2 cutover checks](docs/testing/schema-v2-cutover.md) in a test vault.
